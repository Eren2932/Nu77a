<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the real theming; this is just the launch window. -->
    <style name="Theme.Nuva" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/nuva_background</item>
        <item name="android:navigationBarColor">@color/nuva_background</item>
        <item name="android:windowBackground">@color/nuva_background</item>
    </style>
</resources>
